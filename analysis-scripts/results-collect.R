# generated by generate-commands.py
library(ROCR)
results.final = {}
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-1-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 1, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-2-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 2, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-3-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 3, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-4-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 4, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-5-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 5, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-10-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 10, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-25-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 25, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-50-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 50, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-75-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 75, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-2d/results-100-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 100, "gene.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-1-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 1, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-2-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 2, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-3-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 3, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-4-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 4, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-5-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 5, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-10-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 10, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-25-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 25, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-50-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 50, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-75-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 75, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-gene-3d/results-100-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 100, "gene.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-1-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 1, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-2-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 2, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-3-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 3, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-4-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 4, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-5-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 5, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-10-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 10, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-25-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 25, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-50-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 50, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-75-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 75, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-2d/results-100-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 100, "target.2d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-1-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 1, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-2-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 2, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-3-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 3, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-4-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 4, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-5-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 5, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-10-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 10, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-25-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 25, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-50-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 50, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-75-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 75, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
results = {}
directory = 
for (file in list.files("/Users/Beth/Desktop/results-drug-target-3d/results-100-ebcranksum", full.names=TRUE)) {
	print(file)
	d = read.delim(file, head = F)
	names(d) = c("entitypair", "cooccur", "pgkbstatus")
	pred = prediction(d$cooccur, d$pgkbstatus)
	perf = unlist(slot(performance(pred, measure = "auc"), "y.values"))
	results = c(results, perf)
}
e = data.frame(mean(results > 0.7), mean(results > 0.8), mean(results > 0.9), median(results), min(results), max(results), 100, "target.3d", "ebcranksum")
names(e) = c("fracg07", "fracg08", "fracg09", "median", "min", "max", "S", "datatype", "method")
results.final = rbind(results.final, e)
